  
  export const borderRadius = {
    '10': '.625rem',
    '20': '1.25rem',

    // Primary styles
    xl: 'calc(var(--radius) + 4px)',
    lg: 'var(--radius)',
    md: 'calc(var(--radius) - 2px)',
    sm: 'calc(var(--radius) - 4px)',
  }
  